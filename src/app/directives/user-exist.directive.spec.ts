import { UserExistDirective } from './user-exist.directive';

describe('UserExistDirective', () => {
  it('should create an instance', () => {
    // const directive = new UserExistDirective();
    // expect(directive).toBeTruthy();
  });
});
