import { SameUsernameDirective } from './same-username.directive';

describe('SameUsernameDirective', () => {
  it('should create an instance', () => {
    // const directive = new SameUsernameDirective();
    // expect(directive).toBeTruthy();
  });
});
