import { RetypeDirective } from './retype.directive';

describe('RetypeDirective', () => {
  it('should create an instance', () => {
    const directive = new RetypeDirective();
    expect(directive).toBeTruthy();
  });
});
